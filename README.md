# Quarry

Quarry is a fast, neutral dual-pane file manager for Linux (wxWidgets + C++).

## Build (Ubuntu/Debian)

Dependencies:
- CMake
- A C++20 compiler
- wxWidgets 3.2 development packages (`libwxgtk3.2-dev`)

Build:
```sh
./scripts/build.sh
```

Run:
```sh
./scripts/run.sh
```
